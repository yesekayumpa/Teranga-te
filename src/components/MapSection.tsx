import React from 'react';

/**
 * Google Maps embed is included inside Contact.tsx (just below the form).
 * This file is kept as a no-op for compatibility with the original App.tsx imports.
 */
export const MapSection: React.FC = () => null;
